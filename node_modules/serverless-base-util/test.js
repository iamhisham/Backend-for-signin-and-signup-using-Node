const database = require("./database/Database.js");
database.init().then(() => {
    console.log("DB Initialized Successfully");
}).catch((err) => {
    console.error("Exception occured during db connection", err);
});
